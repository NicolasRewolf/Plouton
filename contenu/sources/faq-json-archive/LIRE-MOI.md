# Archive FAQ JSON (pré-Supabase)

Anciens fichiers `contenu/faq/*.json` — **ne plus lire au runtime**.

La FAQ vit dans Supabase (`public.faq`), importée depuis
`contenu/sources/wix/FAQ.csv` via `scripts/import-faq-csv.py`.

Conservés ici comme sauvegarde historique / diff.
